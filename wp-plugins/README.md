# Drop-in plugins

Put paid or private WordPress plugin **.zip** files here (e.g. WooGraphQL Pro).
The `wp-init` compose service installs and activates every zip in this
directory. Contents are gitignored.
